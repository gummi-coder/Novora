apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shared-service.fullname" . }}-test-connection"
  labels:
    {{- include "shared-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shared-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
