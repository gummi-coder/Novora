apiVersion: v1
kind: Service
metadata:
  name: {{ include "shared-service.fullname" . }}
  labels:
    {{- include "shared-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shared-service.selectorLabels" . | nindent 4 }}
