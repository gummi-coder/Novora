apiVersion: v1
kind: Secret
metadata:
  name: shared-service-secrets
type: Opaque
data:
  # These values should be base64 encoded
  database-url: ${BASE64_DATABASE_URL}
  jwt-secret: ${BASE64_JWT_SECRET}
  redis-url: ${BASE64_REDIS_URL}
  # Add other sensitive configuration as needed 